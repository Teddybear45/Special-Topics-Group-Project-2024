from ultralytics import YOLO
import numpy as np

def predict_image(model: YOLO,
                 image_path: str,
                 result_path: str,
                 only_edges: bool,
                 layer_over: bool):
    pass

def predict_video(model: YOLO,
                  video_path: str,
                  result_path: str,
                  only_edges: bool,
                  layer_over: bool):
    pass

